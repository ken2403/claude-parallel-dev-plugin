{
  "_comment": "hv hooks: language-agnostic, cross-platform. The protected-file guard is the only blocking hook; everything else is advisory.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": ["bash", "${CLAUDE_PLUGIN_ROOT}/scripts/guard-protected.sh"],
            "statusMessage": "hv: checking file is not a secret"
          }
        ]
      }
    ]
  }
}
