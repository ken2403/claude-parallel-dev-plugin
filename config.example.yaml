# Parallel Workspace 設定ファイル

# プロジェクト名 (tmuxセッション名のプレフィックスに使用)
project_name: "my-project"

# 新規ブランチ作成時の派生元ブランチ
base_branch: "main"

# UI モード
#   warp  - Warp terminalでタブを開く
#   tmux  - tmuxセッションのみ（バックグラウンド実行）
ui_mode: "warp"

# Warp terminal URIスキーム (warp または warppreview)
warp_scheme: "warp"
