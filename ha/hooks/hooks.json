{
  "_comment": "ha hooks: language-agnostic. The protected-file guard is the only blocking hook; it refuses edits/writes to secrets.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/guard-protected.sh\"",
            "statusMessage": "ha: checking file is not a secret"
          }
        ]
      }
    ]
  }
}
